# Voice commands for evernote

testing 1 2 3 4= tuna;
Horizontal rule = {CTRL+ shift + -};
Insert table = {Alt}{o}{T};
Attach file = {Alt}{f}{f};
Insert checkbox = {CTRL+ shift + c};

###failed
new Evernote = {CTRL+ALT+N};
watermelon Rebecca=SendSystemKeys({CTRL}{alt}{V});
Evernote copy = HeardWord(press, Windows, a);
